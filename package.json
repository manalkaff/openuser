{
  "name": "openuser-monorepo",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "eslint . --max-warnings 0",
    "typecheck": "pnpm -r typecheck",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "build:release": "pnpm --filter @openuser/shared build && pnpm --filter @openuser/server build && pnpm --filter @openuser/mcp build && pnpm --filter @openuser/ui build && pnpm --filter openuser build && node scripts/copy-ui.mjs && node scripts/copy-skills.mjs && node scripts/copy-migrations.mjs",
    "test:pack": "bash scripts/test-pack.sh"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^8.0.0",
    "@typescript-eslint/parser": "^8.0.0",
    "eslint": "^9.0.0",
    "prettier": "^3.0.0",
    "typescript": "^5.7.0"
  },
  "packageManager": "pnpm@9.15.0"
}
